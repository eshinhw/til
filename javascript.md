# JavaScript

## Destructuring Expression

```
{x,y} = foo;

x = foo.x;
y = foo.y;

{width, height, color} = options

width = options.width; 
height = options.height; 
color = options.color

const { items } = args

const items = args.items
```